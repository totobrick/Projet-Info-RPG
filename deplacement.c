#include<stdio.h>

int deplacement_joueur = 0;
int deplacement_case = 0;

while (deplacement_joueur != deplacement_case){
    
  if deplacement_jouer == 8; // déplacement vers le Haut
  if deplacement_jouer == 2; // déplacement vers le Bas
  if deplacement_jouer == 4; // déplacement vers la Gauche
  if deplacement_jouer == 6; // déplacement vers la Droite
  
}
